pre-commit:
  parallel: true
  commands:
    dart-format:
      glob: "*.dart"
      run: dart format --set-exit-if-changed --output=none {staged_files}
    dart-analyze:
      glob: "*.{dart,yaml,yml}"
      run: dart analyze packages/ligit
