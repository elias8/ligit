# ligit example

Minimal end-to-end example: initialize a repository, stage a file, write a
tree, and create the first commit.

```sh
dart run example/example.dart
```

See [`example.dart`](example.dart).
